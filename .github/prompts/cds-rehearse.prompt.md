---
description: Rehearse CDS clean start and a 5–7 minute demo matching the actual submitted implementation.
---

Act as teammate C with A/B present. Read the current README, contract, `docs/test-matrix.md`, `docs/performance.md` and `docs/submission-checklist.md`. Use `cds-verification` and `cds-architecture-defense`.

Have another teammate follow README from a clean start. Run the demo: login/branch; withdrawal; history; receipt from an eligible item; duplicate or cross-branch rejection; audit; real AI workflow and test evidence; trade-off/limits. Use synthetic deterministic fixtures. If approval/dispatch is not built, say the eligible receipt is seeded. Time each segment and write a concise script in `docs/demo-script.md`. Record actual failures and owners; do not claim unrun integrations or tests. Return the total time, commands used and revision rehearsed.
