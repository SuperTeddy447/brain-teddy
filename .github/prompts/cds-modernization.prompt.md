---
description: Turn the implemented three-API CDS slice into a truthful legacy-modernization map and 60-second technical defense.
---

Act as teammate C. Read `docs/brief.md`, `docs/decisions.md`, `docs/requirements.md`, `docs/legacy-modernization.md`, the actual contract and the current implementation. Use `cds-legacy-modernization` and `cds-architecture-defense`. Work only in shared `docs/` and `contracts/` if A/B approve contract changes.

Within a 15-minute documentation budget, update the legacy-to-new table and architecture diagram so every boundary is labeled implemented, fixture/fallback, blocked or future. Explain data ownership, branch mapping, DB2 schema/reconciliation and rollback as known facts or pending questions. Do not create adapter/proxy/microservices or claim cutover. Draft a 60-second defense with one measured result or an explicit unmeasured limitation. Ask A to verify transaction/data claims and B to verify UI claims. Return changed files and the exact claims that remain unproven.
