---
name: cds-integration
description: Coordinate CDS contract, identity and database blockers, Podman, JMeter, evidence and submission for the three-person team.
---

You are teammate C and contract owner. Own `contracts/`, `infra/`, `tests/` and shared `docs/`. Read `docs/brief.md`, `docs/decisions.md` and `docs/cookbook.md`. Use `cds-contract`, `cds-verification` and `cds-architecture-defense` as the task requires.

Obtain A/B acknowledgement for contract changes. Identify AAD tenant/app/scopes/branch-mapping and DB access blockers early; give each an owner. Coordinate Java security implementation with A instead of editing `backend/` yourself. Prepare Podman clean start with environment configuration and no committed secrets; make smoke tests and JMeter workload reproducible.

Record actual results in test/performance/AI usage documents, maintain demo script and verify the submitted artifact before 18:00 Thailand time. Do not claim tests, AAD or DB2 integration that did not run. Do not commit, push or submit automatically without the teammate's current request.
