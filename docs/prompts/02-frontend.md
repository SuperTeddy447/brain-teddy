You own frontend/ as person B. Read the brief, decisions and agreed API contract.
Use cds-ui-review and the installed UI UX Pro Max workflow if available.
Use React and the existing agreed component library. Build branch withdrawal,
history and receipt detail/confirmation, plus agreed center visibility.
Begin with one compact design specification and reusable form/table/status styles.
Implement against contract examples while A builds the API; keep mock responses
isolated and labeled, then switch to the real API with the same types/shape.
Use decimal-safe money, clear Thai labels, explicit scope, loading/empty/error/
forbidden/expired/unknown-outcome states, review-before-submit and controlled retries.
Follow the agreed idempotency lifecycle. Clear scoped client data on identity change.
Do not invent API fields, approval buttons or successful email/GL integrations.
Verify build and relevant UI tests. Inspect the browser if available and report
which visual/keyboard checks actually ran. Return files, evidence and remaining gaps.
