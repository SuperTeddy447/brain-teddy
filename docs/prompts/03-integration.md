You own infra/, tests/, docs/ and contract coordination as person C.
Read the brief, decisions and API contract. Use cds-verification.
First verify AAD tenant/app registrations, redirect URI, API audience/scopes,
role/branch mapping and test users. Have A implement Java changes in backend/.
If access is missing, record the blocker and owner; do not mark mocked auth complete.
Prepare Podman startup using the environment's real Compose provider, health checks,
persistent volumes and environment configuration without committed secrets.
Run a smoke test for each required API with valid state/branch fixtures.
Create a parameterized JMeter plan with content assertions, a stated workload mix,
valid receipt data, token lifecycle and unique keys where appropriate. Separate
negative tests from load tests. Never hammer interactive AAD login.
Run small loads first if the environment is available. Capture actual JTL/HTML
results and update the performance report, including environment and limitations.
Coordinate the clean-start rehearsal and submission by 18:00 Thailand time.
